# Notemd v1.8.9

## Lo mas destacado

- **La coherencia de la vista previa se ha cerrado hasta los limites del producto real.**：`Vista previa de graficos` Los archivos guardados ahora se volveran a abrir. Mermaid Ruta de origen, por lo que la primera vista previa que aparece despues de la generacion y las vistas previas manuales posteriores estaran en la misma copia. artifact Sobre el valor de verdad。
- **Ventana emergente de vista previa UX Finalmente lo logre frame-safe**：Mermaid La ventana emergente de vista previa ahora usa una barra de operacion vertical, un panel de historial de vista previa y wrapper Control de ancho de nivel, sin desplazamiento horizontal sin sentido en escenas de imagenes pequenas。
- **La liberacion del valor real se completa nuevamente sincronicamente.**：Metadatos de la version del paquete y resumen de actualizacion emergente de bienvenida、README Marcacion de versiones y bilinguismo release notes Alineado con `1.8.9`。

## Nuevas funciones

- en Mermaid Agregado en la ventana emergente de vista previa preview session Historial, los usuarios pueden cambiar directamente a vistas previas graficas abiertas anteriormente.。
- Cambie el boton de operacion de vista previa a un diseno de columna vertical para garantizar que todos los elementos de control sean visibles dentro del marco de la ventana emergente inicial.。
- El resumen de la version en la ventana emergente de bienvenida ha avanzado a `1.8.9` / `1.8.8`。

## Reparacion y Robustez

- Seguimiento `Vista previa de graficos` La llamada ahora vuelve a abrir el contenido fuente guardado en lugar de continuar reutilizando el estado de memoria potencialmente caducado. artifact。
- La ventana emergente de vista previa ahora se controlara simultaneamente Obsidian capa exterior `.modal` shell Eliminado el antiguo valor predeterminado aprox. con ancho de contenido interior 560px wrapper Traiga la barra de desplazamiento horizontal en la parte inferior.。
- Las entradas del historial de vista previa, las etiquetas de ruta del archivo fuente y los botones de operacion ahora admiten ajuste de linea seguro y ya no "luchan por espacio" estirando el diseno.”。
- Ya es real en esta maquina. Obsidian vault Utilizado en `index.zh-CN_summ.md` con `architecture.md` Guardado Mermaid Se reabre la vista previa、history Cambiar con modal Comportamiento dimensional verificado。

## Comparar resumenes de verificacion

- **Antes de los cambios**：La primera vista previa despues de la generacion y las vistas previas manuales posteriores pueden variar porque no necesariamente vuelven a abrir la misma copia guardada. artifact Valor de verdad。
- **Despues de los cambios**：Ambas rutas vuelven a ingresar al limite de la fuente guardada, por lo que el comportamiento es estable y consistente al realizar una vista previa repetida.。
- **Antes de los cambios**：Incluso si el grafico en si es pequeno, la capa exterior Obsidian modal Es posible permanecer escondido aprox. 560px Ancho del caparazon y exposicion de barras de desplazamiento horizontales innecesarias。
- **Despues de los cambios**：wrapper Convergido con el ancho del contenido, probado Mermaid En la escena, la ventana emergente se puede utilizar completamente sin deslizamiento horizontal.。
