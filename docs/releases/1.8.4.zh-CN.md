# Notemd v1.8.4

## Lo mas destacado

- **Provider Solidez y mayor cobertura en China**：Compartir OpenAI-compatible Agregado en tiempo de ejecucion `Xiaomi MiMo` Preestablecer y reforzar endpoint Procesamiento para que los usuarios puedan pegar directamente Provider La direccion de interfaz completa en el documento ya no provoca errores de empalme de ruta.。
- **Barreras de seguridad del flujo de trabajo de la nota conceptual**：Complete las indicaciones de configuracion previa para comandos y flujos de trabajo que dependen de la generacion de notas conceptuales para evitar seguir generando productos erroneos cuando la ruta de la nota conceptual no esta configurada correctamente.。
- **Extraiga el texto original especificado en lotes y refuerce la interaccion con la pagina de configuracion.**：Se agrego la capacidad por lotes de "Extraer texto original especificado" y se soluciono el problema de regresion donde el cuadro de entrada de configuracion pierde foco inmediatamente despues de ingresar un numero.。
- **Alinear la publicacion, la documentacion y la higiene del espacio de trabajo.**：Actualizar release/docs Valor verdadero, proteccion local. `.trellis` Estado del flujo de trabajo para garantizar que el comportamiento actual de la linea principal sea coherente con los activos liberados.。

## Nuevas funciones

- Agrega un nuevo nivel `Xiaomi MiMo` Preestablecido, incorporado MiMo Configuracion predeterminada（`https://api.xiaomimimo.com/v1`、`mimo-v2.5-pro`），Y seguir reutilizando la accion OpenAI-compatible Enlace de llamada。
- Barra lateral / Se agrego un nuevo directorio de acciones de flujo de trabajo **Extraccion por lotes de textos originales especificos.**，Todos los archivos de la carpeta seleccionada se pueden `.md` / `.txt` El archivo ejecuta el mismo conjunto de configuraciones de preguntas.。
- Se agrego una ventana emergente de advertencia de ruta de nota conceptual para agregar enlaces. / Proporcionar procesos relacionados con la extraccion de conceptos. **Configurar**、**No hay aviso esta vez**、**No vuelvas a preguntar** Tres opciones。
- La ventana emergente de bienvenida para la primera instalacion ahora tiene resumenes de actualizacion integrados de las dos ultimas versiones y proporciona un area desplazable independiente para ver contenido mas extenso.。
- Se ha ajustado el orden de los elementos de configuracion clave: la salida de la nota conceptual y los parametros de procesamiento se han adelantado Translate Anteriormente, el modo de consulta combinado se incluia en la configuracion de la tarea "Extraer texto original especificado".。

## Reparacion

- OpenAI-compatible endpoint La normalizacion ahora incluira `/chat/completions` o `/models` de Base URL，Evite fallas de solicitud causadas por empalmes repetidos。
- Anteriormente se activaria durante la entrada. `display()` Los elementos de configuracion redibujados ahora se cambian a blur / Enter Envielo al mismo tiempo, solucione el problema desenfocado de "ingrese un numero y aparecera inmediatamente" y cubra los campos afectados restantes.。
- `Personaliza la ruta de la nota conceptual` con `Habilitar Mermaid Deteccion de errores` Ahora habilitado de forma predeterminada, esta mas en linea con las notas conceptuales de dependencia y Mermaid Ruta de uso real del posprocesamiento。
- `Maximo Token contar` Avanzo `Numero de palabras fragmentadas` antes，`Numero de palabras fragmentadas` Ahora indica claramente que el valor recomendado es aproximadamente un tercio del anterior y completa automaticamente el valor recomendado si el usuario no lo ha personalizado.。

## Varios

- renovado README、Arquitectura, auditoria de progreso y documentacion de lanzamiento para realinear el codigo actual, los limites de lanzamiento y la direccion de avance posterior.。
- Ya `.trellis/` Inclusion `.gitignore`，y en `AGENTS.md` Agregue local conciso Trellis Requisitos de proteccion del estado para evitar la sincronizacion posterior o la eliminacion accidental de la limpieza。
