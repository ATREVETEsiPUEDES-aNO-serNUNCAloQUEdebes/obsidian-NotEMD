# Notemd v1.9.2

## Lo mas destacado

- **Esta version cubre el extremo remoto. `1.9.1` Incrementos posteriores efectivos de la linea principal**：El siguiente contenido se basa en los envios. `3c90553..1cb58cb` Generacion de intervalos, es decir, desde la historia remota. `1.9.1` release cut hasta el presente `main` La verdadera diferencia。
- **Sidebar El diseno de observabilidad vuelve a estar disponible.**：Area de desplazamiento inferior y API activity El estilo ha sido restaurado y la informacion de la actividad ya no esta Log output Exprimir fuera de la vista。
- **Base de conocimientos locales inspect Mas diagnostico, pero no amplia el alcance del comportamiento en tiempo de ejecucion.**：mantenedor inspect Ahora volvera acotado query diagnostics，Y dar notas de navegacion con poca senal. caution，En lugar de simplemente exponer a un debil query Resultados。

## Actualizaciones importantes

- Se agrego "Division de capitulos". + TOC”Documentos de enfoque independientes y representaciones en chino e ingles. SVG Con el ingreso de documentos, esta funcion ingresa oficialmente a la superficie del documento de entrega visible en el almacen y ya no existe solo en el contexto del mantenedor.。
- Extensiones `local-knowledge.inspect` de explainability，Agregar nueva estructura `queryDiagnostics`：Incluyendo derivados basename、diagram-source Consultar el uso real de acotado stripped-source Numero de caracteres, y `index.*` Este tipo de nota de navegacion se denomina caution。
- Pon al mantenedor en el almacen. CLI Ayuda, ejemplos y metodos. capability matrix realinearse a `--vault docs` Se requiere verdad vault-relative Contrato de ruta para reducir la diferencia entre ejemplos de documentos y llamadas reales。
- Apretar release Mantener el verdadero valor del vinculo：chronicle refresh helper La identidad del mantenedor ahora se conservara y el envio de actualizacion del documento basado en la version ya no se escribira como bot Identidad。

## Reparacion y Robustez

- Fijo sidebar El comportamiento de desplazamiento del area inferior hace que Log output Con API activity Puede desplazarse verticalmente normalmente dentro del mismo panel de envio en lugar de competir por una altura fija。
- Fijo docs vault Base central de conocimientos locales inspect Ejemplo previamente usado incorrectamente `docs/...` repo-relative El problema de la ruta ahora esta unificado como `index.zh-CN.md` / `maintainer` Este tipo vault-relative Entrada。
- mantenedor fijo CLI help Salida de ejemplo minima para que sea coherente con el enfasis en tiempo de ejecucion vault-relative Reglas del camino。
- Se corrigio la base de conocimiento local en escenarios de notas de navegacion con baja senal. inspect El problema de la interpretabilidad insuficiente: ahora la salida de prueba y tiempo de ejecucion se proporcionara explicitamente caution，En lugar de permitir que este tipo de resultados de recuperacion debiles continuen en el estado de "el resultado esta vacio pero se desconoce el motivo".。

## Resumen comparativo

- **Antes de los cambios**：sidebar en request activity Despues de la reversion del estilo，API Las superficies de observacion conjuntas con troncos se vuelven mas dificiles de usar.。
- **Despues de los cambios**：El area inferior recupera limites de desplazamiento claros.，API activity La secuencia de registro ya no estara bloqueada.。
- **Antes de los cambios**：`local-knowledge.inspect` Aunque derivado query，Pero no hay suficiente evidencia limitada para explicar basename / diagram-source query ¿Por que es debil?。
- **Despues de los cambios**：inspect permanecer maintainer-only，Pero ahora vuelve acotado query diagnostics Con navigation-note caution，Facilitar la localizacion de enlaces de recuperacion de senal baja.。
- **Antes de los cambios**：mantenedor CLI Documentacion en `--vault docs` El ejemplo implica repo-relative Ruta, que no coincide con las expectativas reales del tiempo de ejecucion.。
- **Despues de los cambios**：Los ejemplos, los resultados de ayuda y las pruebas convergen a lo real. vault-relative Contrato para reducir errores operativos durante la verificacion local。

## Verificacion

- `npm run chronicle:sync-repo-saga`
- `npm run chronicle:update -- --tag 1.9.2`
- `npm run build`
- `npm test -- --runInBand`
- `npm run audit:i18n-ui`
- `npm run audit:render-host`
- `obsidian help`
- `obsidian-cli help`
- `git diff --check`
