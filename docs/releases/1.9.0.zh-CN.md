# Notemd v1.9.0

## Lo mas destacado

- **Este es un completo `1.8.9 -> 1.9.0` Resumen de lanzamiento acumulativo**：En linea `1.9.0` release notes Ahora se cubre todo el intervalo de entrega, en lugar de solo la ultima seccion. advanced batch Incremento。
- **Notemd Ahora tiene una clara convergencia CLI / public-surface Arquitectura**：Basado en registry de operation Metadatos, mecanografia capability/contract Exportacion y desensibilizacion provider profile Exportar y repo-local maintainer helper，Trabajar juntos para definir claramente los limites de la automatizacion, en lugar de pretender ser "integral" public CLI”。
- **La generacion y vista previa de graficos se ha transformado de experimentos dispersos en sectores de plataforma verificables.**：spec-first Generacion de multiples objetivos, guardada artifact Vista previa del valor real、history、Exportar enlaces y verificacion semantica. helper Ya combinado e implementado。
- **Stage C El flujo de trabajo del conocimiento se ha fortalecido significativamente.**：Busqueda en la base de conocimientos local, contrato dividido por capitulos, avanzado batch Los archivos de seleccion de archivos y la relajacion de las restricciones de los archivos de entrada han dado como resultado contratos de tiempo de ejecucion mas seguros y una mejor visibilidad.。

## Actualizaciones importantes

- Ampliado provider/runtime Apoyo y estabilidad, incluida una mayor disponibilidad de China provider Diagnostico de solicitud largo y predeterminado、streaming Percibido fallback Vinculos y conexiones mas defensivas/Reintentar la logica。
- Completado a gran escala. i18n Cierre: cubrimiento settings、notice、workflow surface、sidebar action、preview chrome、welcome/onboarding Redaccion publicitaria y todo el paquete multilingue. README。
- Aterrizo registry-backed de CLI Capa de extraccion：operation registry、capability manifest Exportar、invocation contract Derivada, convergente public-safe surface Exportar、provider/config profile Ordenes, y repo-local maintainer invoke/help bridge。
- Se completaron las capacidades de la plataforma de graficos.：spec-first Mermaid Generar、JSON Canvas con Vega-Lite Metas、renderer/cache/preview Tuberia, guardada artifact Reabrir vista previa、preview history、La copia de operacion visible despues de la unificacion, asi como la `diagram.generate` Abajo typed follow-through Capa de resultados。
- Se agrego semantica a nivel de mantenedor y verificacion de empaquetado.：render-host bundle audit、`npm run verify:diagram-semantics`、De build/release Derivacion del valor de verdad packaging-contract comprobar、numeric tag Restricciones y chronicle/release workflow Endurecimiento。
- Procesamiento de notas y flujo de trabajo de conocimientos ampliados: la busqueda de la base de conocimientos local se ha conectado a la generacion de titulos por lotes, investigacion y resumen, y generacion de graficos; La division de capitulos ha sido determinista. TOC artifact con guarded rerun Semantica；batch Tambien se han implementado la persistencia del progreso y el almacenamiento en cache de respuestas.。
- Entrega avanzada batch Seleccion de carpeta y liberacion del control de entrada: se puede nombrar file-selection profiles、regex/glob Reglas、`relativePath` / `basename` Control de alcance de destino y subcarpeta、preset chips、Sobrescritura unica, vista previa del archivo seleccionado y solo allowlist La tarea esta abierta. PDF Formula de extraccion relaxed-input reader。

## Reparacion y Robustez

- Limite del valor de verdad de vista previa unificada: manual `Vista previa de graficos` Los archivos guardados ahora se volveran a abrir. artifact fuente sin desviarse de la primera vista previa tras generacion。
- Mejorado Mermaid Compatibilidad: Incluida parse-error retry、pipe label escapar、best-effort Comportamiento de guardado, explicito. intent routing，Y mas seguro legacy/fixer Dividir fronteras。
- Al compartir host-adapter con operation contract，Reducido el panel de mando.、sidebar、folder-task host、maintainer helper con path-based operation Deriva entre。
- voluntad backup Restaurar los sectores delimitados en las ramas a la situacion actual. `main`，Volver a auditar packaging Valor verdadero, proteger local `.trellis/` Estado y mantenimiento despues de la recuperacion. release/docs Sincronizacion de la verdad。
- Higiene automatizada mejorada: check-in bilingue release notes、Requerido release assets、Numeros puros release tag、repo-saga Bloqueo de serie, normalizacion del colaborador y solo escaneo tracked files Restricciones de verificacion de documentos。

## Comparar resumenes de verificacion

- **Antes de los cambios**：Publicado `1.9.0` notes Cubre solo el ultimo parrafo advanced batch Segmentacion de entrada, que claramente subestima lo que realmente ofrece esta version CLI、Alcance del refuerzo de graficos, recuperacion, empaquetado y tiempo de ejecucion。
- **Despues de los cambios**：En el almacen y GitHub en `1.9.0` notes Ahora completamente cubierto `1.8.9..1.9.0` Intervalo de acumulacion, al mismo tiempo. release helper de repair path Tambien se basara en el bilinguismo en el almacen. notes La reescritura ya existe release de body。
- **Entregado `1.9.0` Verificacion funcional del conjunto de productos.**：Completado previamente repo-saga Actualizacion en serie、`npm run build`、`npm test -- --runInBand`、`npm run audit:i18n-ui`、`npm run audit:render-host`、`obsidian help`、`obsidian-cli help`、repo-local `obsidian build`、repo-local `obsidian test`、`obsidian-cli doctor` con `git diff --check`。
- **Esta reparacion pass** Revision adicional release-note repair Proceso、GitHub release body，Y sincronizacion release Post-documentacion clean git state。
