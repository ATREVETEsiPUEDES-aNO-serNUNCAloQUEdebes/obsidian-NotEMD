# Notemd v1.8.7

## Lo mas destacado

- **Tareas de carpeta selector Convergencia**：Procesamiento a nivel de carpeta/Extraccion/Traduccion/La ruta de reparacion se unifico y reutilizo como un conjunto de contratos de seleccion de archivos y ya no se implementa por separado.。
- **operation Anular la alineacion de parametros**：El parametro por cobertura de la tarea de carpeta ya esta en canonical host-adapter Apoyo unificado en el camino, incluyendo `Extraccion por lotes de textos originales especificos.`。
- **Publicar sincronizacion de la verdad.**：Metadatos de la version del paquete y resumen de la ventana emergente de bienvenida de las dos ultimas actualizaciones.、README Marcacion de versiones y bilinguismo release notes Alineado con `1.8.7`。

## Nuevas funciones

- Compartir tareas de carpeta selector Apoyo contractual：
  - Modo de filtro：`No filter` / `Contains` / `Regex` / `Glob`
  - Filtrar objetivos：`Relative path` / `Basename`
  - Coincidencia inversa que distingue entre mayusculas y minusculas
  - Modo de compatibilidad de subdirectorios (predeterminado `legacy` Mantenga las tareas de traduccion no recursivas a menos que el usuario las cambie explicitamente a include）
- Tareas de carpeta operation Los parametros de cobertura de entrada se han pasado a host-adapter Ruta de ejecucion unificada。
- `content.batch-extract-original-text` de operation Registro, entrada/Salida schema Completado y alineado con el contrato de lista de capacidades.。

## Reparacion y Robustez

- Introducir el intercambio regex Verificacion helper `getFolderTaskRegexValidationError`，Y se utiliza para configurar la verificacion previa de la pagina y el tiempo de ejecucion al mismo tiempo. matcher Logica para eliminar el riesgo de una doble deriva en la implementacion。
- aumentar host-adapter Pruebas de regresion, bloqueo：
  - `folderPathOverride + fileSelectionOverride` Semantica de ejecucion de
  - Proceso de sintesis de parametros de cobertura. base settings No contaminado
- La pagina de configuracion esta en regex La ilegalidad de la localizacion se puede dar antes en el modo. pattern Aviso, manteniendo el comportamiento de "edicion sin bloqueo" y reduciendo el costo de demora de los "errores descubiertos en tiempo de ejecucion"。

## Comparar resumenes de verificacion

- **Antes de los cambios**：La logica de filtrado de tareas de carpeta esta dispersa y existe el riesgo de una posible desviacion en la configuracion de las indicaciones y la semantica del tiempo de ejecucion.。
- **Despues de los cambios**：selector Contrato unificado, verificacion de configuracion y semantica de coincidencia de tiempo de ejecucion，regex La verificacion es compartida por helper Bloquear la coherencia。
- **Antes de los cambios**：operation Alineacion insuficiente de las superposiciones de carpetas de nivel en diferentes flujos de trabajo。
- **Despues de los cambios**：canonical host-adapter Los parametros de cobertura se han aplicado uniformemente a la ruta y el contrato de extraccion del texto original del lote se ha cubierto claramente.。
- **Estabilidad**：`includeSubfolders = legacy` El alcance predeterminado de la siguiente tarea de traduccion permanece sin cambios para evitar la regresion del comportamiento historico.。
