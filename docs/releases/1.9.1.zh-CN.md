# Notemd v1.9.1

## Lo mas destacado

- **Esta version cubre la totalidad `1.9.0 -> 1.9.1` Intervalo de acumulacion**：El siguiente contenido esta basado en hechos reales. `git diff 1.9.0..HEAD` Genera en lugar de simplemente describir el ultimo. patch O la ultima vez provider-settings Enviar。
- **Provider Configuracion mas sencilla sin perdida de capacidad.**：De forma predeterminada, solo se muestran los campos de conexion necesarios, mientras que las anulaciones avanzadas existentes se conservan y se pueden seguir utilizando.。
- **Las capacidades de descubrimiento de modelos son mas amplias y los limites son mas seguros.**：`Obtener lista de modelos` Ahora admite mas provider catalog Dale forma, pero encuentra el resultado. token El limite superior solo se escribira en el actual provider de override，No sobrescribira el tiempo de ejecucion global. token Presupuesto。
- **token Limite superior fallback Ahora es fail-closed**：Cuando rio arriba catalog No se puede proporcionar credibilidad max-output-tokens tiempo，Notemd Se dara prioridad a conservar los existentes y validos. provider override，Vaya solo si no hay un valor de anulacion valido fallback，Y solicitar claramente a los usuarios que revisen manualmente。

## Actualizaciones importantes

- Limitado extendido provider model discovery，Cubrir mas OpenAI-compatible endpoint family、Variantes de puerta de enlace alojada y extras response envelope、Paginacion/continuation Forma, y provider-specific catalog Formato。
- mantener provider settings Adopcion del Grupo Especial metadata-driven El nucleo de/Capas avanzadas manteniendo las avanzadas guardadas provider Indique y permita que los bloques avanzados se comporten de manera consistente al abrir la configuracion varias veces。
- Reforzado model-aware token guidance，Dejalo claro a los usuarios：
  - provider Salida de nivel token override
  - Panorama general `MaximoTokencontar`
  - Recomendado `Numero de palabras fragmentadas`
- Mantener el "modelo de edicion manual" actual aun puede impulsar la gestion automatica global token Comportamiento basico, al tiempo que limita claramente la capacidad de “descubrir modelos y aplicarlos” a provider override Canal。
- Ampliado Stage-C Flujo de trabajo de la base de conocimientos local para habilitar fuentes de la base de conocimientos a nivel de tarea、inspect/evaluation Visibilidad y fracaso/El comportamiento en algunos escenarios disponibles es mas claro.。
- Se fortalecio el enlace de verificacion del mantenedor del grafico. semantic gate Cubrir, continuar dejando renderer-boundary con packaging-boundary Verifique la alineacion con el contrato de ejecucion de entrada unica actual。
- Sigue asi packaging En potencial render-host runtime lane en fail-closed，Evita el presente `main` Volviendo a “entregar a medias” otra vez detached runtime topology”。
- Limitado retenido maintainer CLI/public-surface Camino, incluyendo repo-local `obsidian` / `obsidian-cli` helper Alineacion y check-in release-helper Cobertura del contrato。
- Se resincronizo el lado de la verdad para su publicacion: metadatos de la version, resumen de las dos ultimas actualizaciones en la ventana emergente de bienvenida, bilinguismo. release notes、changelog con README Los marcadores familiares ahora estan alineados con `1.9.1`。

## Reparacion y Robustez

- Fijo discovered-model apply Confundir el limite superior de la produccion del modelo con la situacion global `MaximoTokencontar` Problemas de sincronizacion。
- Se soluciono el problema de que no se puede analizar el flujo ascendente. discovered-model ceiling Borrara o degradara los existentes provider override Pregunta。
- Se corrigio la actualizacion de la version del resumen de actualizacion reciente en la ventana emergente de bienvenida, que ahora se mostrara en el idioma local. `1.9.1` con `1.9.0` Resumen correcto de。
- Fijo provider El comportamiento de persistencia de los bloques avanzados evita que las configuraciones avanzadas existentes no predeterminadas se doblen mal y sean dificiles de descubrir despues de volver a abrir las configuraciones.。
- Se corrigio la ventana emergente de bienvenida. release notes Logica de seleccion de localizacion para evitar recurrir a resumenes en ingles obsoletos。
- Realinear los documentos de progreso y los documentos del programa a los actualmente entregados. provider-settings Arquitectura, esp. provider override、Descubrimiento de modelos acotados y token fallback Semantica。

## Resumen comparativo

- **Antes de los cambios**：Descubrimiento de modelos y token guidance Todavia existe una frontera difusa entre discovered-model apply Entendido como "maximo global sincronicoTokenRuta "Numera"。
- **Despues de los cambios**：El comportamiento entregado ahora es explicito y limitado. El descubrimiento del modelo solo ceiling Aplicar a la actual provider override；El presupuesto de tiempo de ejecucion global aun se controla mediante un seguimiento manual basado en el modelo, a menos que el usuario se desvie activamente.。
- **Antes de los cambios**：aguas arriba token ceiling Cuando falta, ya hay una valida provider-specific override Puede ser mas debil fallback Cobertura。
- **Despues de los cambios**：fallback Existir solo como ultimo recurso. Si ya existe una valida provider override，siempre se conserva; de lo contrario, se utiliza el valor predeterminado conservador y se requiere explicitamente que el usuario verifique manualmente。

## Verificacion

- `npm run build`
- `npm test -- --runInBand`
- `npm run audit:i18n-ui`
- `npm run audit:render-host`
- `obsidian help`
- `obsidian-cli help`
- Pase `obsidian-cli native` Ejecutar esta maquina Obsidian Recarga en caliente y verificacion del tiempo de ejecucion
- `git diff --check`
